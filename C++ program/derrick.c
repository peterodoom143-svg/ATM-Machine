#include <iostream>

int main()
{
   //if statement=do something if a condition is true.
   //              if not,then don't do it.

   int age;

   std::cout << "enter your age: ";
   std::cin >> age;

   else if(age >= 100){
     std::cout << you are too old enough to enter this site!";
   }
   else if(age >= 18){
     std::cout << "welcome to the site!";
   }
   else if(age < 0){
     std::cout << "You have'nt been born yet!";
   }
   else if(age < 18){
     std:: << "You are not old enough to enter!";
   }

   return 0;
{




